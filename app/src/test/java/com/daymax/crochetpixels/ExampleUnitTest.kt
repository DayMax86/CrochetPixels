package com.daymax.crochetpixels

import com.google.gson.Gson
import com.google.gson.reflect.TypeToken
import org.junit.Test
import java.io.File

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class GetDataUnitTest {
    @Test
    fun getGSONfromJSON() {
//        val path = "/Users/maxday/AndroidStudioProjects/CrochetPixels/app/src/main/java/com/daymax/crochetpixels/data.json"
//        val data = File(path).readText(Charsets.UTF_8)
//        val gson = Gson()
//        val responseType: TypeToken<MutableList<DataCell>> = object : TypeToken<MutableList<DataCell>>(){}
//        val dataCells: MutableList<DataCell> = gson.fromJson(data, responseType)
//        assert(dataCells.size == 6400)
    }
}